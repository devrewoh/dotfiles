# Minimal tmux configuration - XDG compliant with basic usability
# Keeps tmux mostly default but adds mouse support and XDG compliance

# XDG Base Directory compliance
set-environment -g TMUX_PLUGIN_MANAGER_PATH "$XDG_DATA_HOME/tmux/plugins"

# Enable mouse support (for scrolling and clicking between panes/windows)
set -g mouse on
